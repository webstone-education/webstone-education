import { courseLanguages } from "./translations/languages";
export { rtlLanguages } from "./translations/languages";

export const allLanguages = courseLanguages;

/**
 * Map of language codes to a written out language name.
 * Used to populate the language switcher in the navbar.
 */
export default courseLanguages;
